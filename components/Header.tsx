import React from "react";
import { Header, Body, Title } from 'native-base';

export default function PlayList() {
  return (
    <Header>
      <Body>
        <Title>ActOne</Title>
      </Body>
    </Header>
  );
}
